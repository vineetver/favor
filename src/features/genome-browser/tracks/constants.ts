// src/features/genome-browser/tracks/constants.ts
// Shared constants for track specs

/**
 * Linking ID used by every track that should pan/zoom together.
 * Master uses "link1"; we keep the same value so URLs and behavior match.
 */
export const LINKING_ID = "link1";
